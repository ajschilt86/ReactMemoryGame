import React from "react";
import "./Title.css";

const Title = props => (
    <div className="titleBox">
        <h1>DC Universe Memory Game</h1>
        <h6>Click on an image to earn points but do not click on the same image twice!</h6>
    </div>
);

export default Title;
